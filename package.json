{
  "name": "redux-navigator",
  "version": "0.0.2",
  "description": "router for redux-based applications",
  "main": "build/redux-navigator.js",
  "scripts": {
    "test": "tape test.js",
    "cover": "istanbul cover test.js",
    "watch": "watchify -v -e router.js -t [ babelify --presets [ es2015 ] ] --debug -o build/redux-navigator.js",
    "build": "NODE_ENV=production browserify -v -e router.js -t [ babelify --presets [ es2015 ] ] -o build/redux-navigator.js",
    "clean": "rm build/redux-navigator.js"
  },
  "author": "Max Harris <harris.max@gmail.com> (http://nodesource.com/)",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "babelify": "^7.3.0",
    "browserify": "^13.1.1",
    "istanbul": "^0.4.5",
    "qs": "^6.3.0",
    "routes": "^2.1.0",
    "tape": "^4.6.3",
    "watchify": "^3.8.0"
  }
}
