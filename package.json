{
  "name": "redux-navigator",
  "version": "0.0.7",
  "description": "router for redux-based applications",
  "main": "redux-navigator.js",
  "scripts": {
    "test": "tape test.js",
    "cover": "istanbul cover test.js",
    "watch": "babel lib/router.js --watch --presets=es2015 --out-file redux-navigator.js",
    "build": "babel lib/router.js --presets=es2015 --out-file redux-navigator.js",
    "clean": "rm redux-navigator.js"
  },
  "author": "Max Harris <harris.max@gmail.com> (http://nodesource.com/)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "tape": "^4.6.3"
  },
  "dependencies": {
    "history": "^4.5.0",
    "qs": "^6.3.0",
    "routes": "^2.1.0",
    "url": "^0.11.0"
  }
}
